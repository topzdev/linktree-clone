import React from "react";
import DashboardThemesPage from "@/app/dashboard/(main)/themes/_components/DashboardThemesPage";

type Props = {
    children?: React.ReactNode
}

const Page = (props: Props) => {
    return <DashboardThemesPage/>
}

export default Page;